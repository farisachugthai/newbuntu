#!/bin/bash

# brought to you in part by the random flash of remembering what lsb_release was
# and by the new tldr command i've downloaded that's >>>> tldr.py

lsb_release -i
Distributor ID:	Ubuntu

lsb_release -r
Release:	18.04

uname -m
x86_64

lsb_release -rs
18.04

lsb_release -is
Ubuntu

# these are gonna be so useful in checking whether someone's on the right OS or distro!
